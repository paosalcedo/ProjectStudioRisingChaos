using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerAlertStrings : MonoBehaviour {

	public static string hitAlert;
	// Use this for initialization
	public static void Start () {
		
	}
	
	// Update is called once per frame
	// public static void UpdateHitAlertText(string targetHit){
	// 	if(CurrentPlayerTracker.currentPlayer.GetComponent<StealthPlayerSwitcher>().myIndex == 0){
	// 		hitAlert = "You hit " + PlayerNames.playerOneName;
	// 	}
	// 	if(CurrentPlayerTracker.currentPlayer.GetComponent<StealthPlayerSwitcher>().myIndex == 1){
	// 		hitAlert = "You hit " + PlayerNames.playerTwoName;
	// 	}
	// }
}
